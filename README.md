* Install hubot.
* Clone this repo inside hubot.
* Run `npm install ./hubot-jira-webhooks --save`.
* Delete the newly created node_module. `rm -rf node_modules/hubot-jira-webhooks`.
* Create a symlink to your repo `cd node_modules && ln -s ../hubot-jira-webhooks`.
